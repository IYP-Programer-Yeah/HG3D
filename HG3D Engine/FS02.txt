#version 450

layout(early_fragment_tests) in; //later will be added if it was availble

in vec3 normal;
in vec2 coord;

out vec4 Output1;
out vec4 Output2;
out unsigned int Output3;

uniform unsigned int MatID;
uniform sampler2D Diffuse_Texture;

void main()
{
	Output1=vec4(normalize(normal.xyz)/2.0f+0.5f,0.0f);
	Output2=vec4(texture(Diffuse_Texture,coord).xyz,0.0f);
	Output3=MatID;
}
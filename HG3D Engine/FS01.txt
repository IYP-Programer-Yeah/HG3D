#version 420

#define VSM

layout(early_fragment_tests) in; //later will be added if it was availble

#ifndef PCF
out vec2 Output1;
#else
out float Output1;
#endif

void main()
{
#ifndef PCF
	Output1=vec2(gl_FragCoord.z,gl_FragCoord.z*gl_FragCoord.z);
#else
	Output1=gl_FragCoord.z;
#endif
}
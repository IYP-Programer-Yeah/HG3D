#version 330

#define PCF

//layout(early_fragment_tests) in; //later will be added if it was availble

#ifndef PCF
in vec4 Actual_Pos;
#endif

out vec2 Output1;

void main()
{
#ifndef PCF
	vec4 normalized_pos=Actual_Pos/Actual_Pos.w/2+vec4(0.5,0.5,0.5,0.5);
	Output1=vec2(normalized_pos.z,normalized_pos.z*normalized_pos.z);
#endif	
}